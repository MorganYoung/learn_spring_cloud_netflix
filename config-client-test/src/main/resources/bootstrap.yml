spring:
  application:
    name: client1
  cloud:
    config:
      profile: dev
      uri: http://localhost:8886/
      name: test-cfg1

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/